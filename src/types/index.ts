export interface ServiceType {
  id: number;
  title: string;
  description: string;
  price: string;
  icon: string;
}

export interface TestimonialType {
  id: number;
  name: string;
  text: string;
  rating: number;
  image: string;
}